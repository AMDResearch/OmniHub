# Sample DeepSpeed Configuration Files

This directory contains some sample DeepSpeed configuration files. They are provided for reference purposes and users
may add or edit them as needed. They may refer to them in their application configuration files with paths relative to
the config directory (e.g., `ds_configs/zero2.json`).
